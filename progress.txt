## Codebase Patterns

- Builtin types are defined in `src/core/compile/builtin/` with separate modules for prelude, python, etc.
- Each builtin module has a `namespace()` function that returns a BTreeMap of names to NamespacedObjects
- The `Builtin` enum in `mod.rs` wraps all builtin variants and `match_builtin!` macro dispatches to them
- Type helpers like `Ty::prelude()` and `Ty::python()` are in `src/core/compile/check/mod.rs`
- Module registration happens in `src/core/preprocess/mod.rs` with the `Module` enum
- Generated code templates are in `src/core/generate/mod.rs` using `quote!` macro
- Data files live in `data/const/` and are included via macros in `src/data.rs`
- **E2E workflow**: `seahorse init` → write .py → `seahorse compile` → split output files → `anchor build`
- **blake3 compatibility**: Pin blake3 to =1.8.2 in generated Cargo.toml (v1.8.3+ requires edition 2024 unsupported by Solana BPF toolchain)
- **Reference programs**: Find idiomatic Anchor programs first, then write Seahorse equivalents to match
- **Testing stack**: Mollusk for unit tests, LiteSVM for integration tests, TypeScript secondary

- **Solana SDK versioning**: Use solana-sdk 2.2.x with mollusk-svm 0.1.x for Anchor 0.32 compatibility. SDK 3.0+ is only available in the modular crate ecosystem (solana-account, solana-pubkey, etc.) and requires Anchor 1.0+
- **Mollusk versions**: 0.0.x uses solana-sdk 2.1, 0.1.x uses solana-sdk 2.2, 0.8+ uses modular crates v3.x
- **Mollusk API (v0.1.x)**: Uses Account type (not AccountSharedData). Need ReadableAccount trait for .lamports()
- **Anchor discriminators**: Use sha256("account:<AccountName>")[..8] for accounts, sha256("global:<instruction_name>")[..8] for instructions
- **LiteSVM versions**: 0.3.x uses solana-sdk ~2.0, 0.4.x-0.5.x uses solana-sdk 2.1, 0.6.x uses modular crates 2.2, 0.7+ uses modular crates 3.x
- **LiteSVM API (v0.6.x)**: Uses solana modular crates (solana-account, solana-pubkey, etc.) at 2.2.x. TransactionResult is Result<TransactionMetadata, FailedTransactionMetadata>
- **anchor-litesvm compatibility**: v0.2.0 requires anchor-lang 0.31.1, v0.3.0 requires anchor-lang 1.0-rc. Neither compatible with Anchor 0.32.1.
- **TypeScript tests**: Located in tests/ts/, run with `npm test` from that directory. Uses litesvm 0.4.0 npm package with vitest.
- **Build pipeline**: `scripts/build-test-programs.sh` compiles Seahorse examples → builds .so files → outputs to target/deploy/
- **Seahorse compiled output format**: Multi-file with markers `// ===== dot/mod.rs =====`, `// ===== dot/program.rs =====`, `// ===== lib.rs =====`
- **Mutable wrapper in Seahorse**: Compiled code wraps accounts in `Mutable<T>` requiring `.borrow()` for field access in PDA signer seeds
- **Program IDs**: Ensure base58 program IDs decode to exactly 32 bytes (valid base58 address length)

---

# Progress Log

## 2026-01-20 - US-005
- Implemented Mollusk test harness in tests/unit/
- Files changed: tests/unit/Cargo.toml, lib.rs, helpers.rs, tests.rs, .gitignore
- **Learnings for future iterations:**
  - mollusk-svm v0.0.12 is compatible with solana-sdk 2.1 (Anchor 0.32 compatible)
  - v0.8+ and v0.10+ use Solana SDK v3/v4 modular crates - incompatible with current Anchor
  - AccountSharedData requires importing ReadableAccount trait for .lamports() method
  - Mollusk's Check API: Check::success(), Check::account(&key).lamports(amt).build()
  - System program tests work out-of-box with Mollusk::default()
---

## 2026-01-21 - US-000
- Upgraded Solana SDK and Mollusk to latest compatible versions
- Files changed: tests/unit/Cargo.toml, tests/unit/tests.rs
- Changes made:
  - solana-sdk: 2.1.0 → 2.2.1
  - solana-bpf-loader-program: 2.1.0 → 2.2.1
  - solana-compute-budget: 2.1.0 → 2.2.1
  - solana-program-runtime: 2.1.0 → 2.2.1
  - solana-system-program: 2.1.0 → 2.2.1
  - mollusk-svm: 0.0.12 → 0.1.3
- API migration: AccountSharedData → Account in test code
- **Learnings for future iterations:**
  - Solana SDK 3.0 refers to the NEW modular crate ecosystem (solana-account v3, solana-pubkey v4, etc.)
  - The monolithic solana-sdk crate jumps from 2.x to 3.0 with breaking changes (Address type replaces Pubkey)
  - Anchor 0.32.x uses modular crates at v2.x - NOT compatible with SDK 3.0+
  - Full SDK 3.0 support requires waiting for Anchor 1.0 release
  - mollusk-svm 0.1.x is the latest version using monolithic solana-sdk 2.2
  - mollusk-svm 0.8+ uses modular crates and is incompatible with current Anchor
  - The practical "SDK upgrade" for Anchor 0.32 compatibility is solana-sdk 2.2.x
---

## 2026-01-21 - US-001
- Updated Anchor dependencies for SDK 3.0 compatibility
- Files changed: src/bin/cli/init.rs
- Changes made:
  - Removed outdated ahash pin (=0.8.9) - no longer needed with Rust 1.84+ in Solana platform-tools v1.51
  - Updated pyth-sdk-solana from 0.10.2 to 0.10.6
  - Generated Cargo.toml now inherits anchor-lang version from `anchor init` (currently 0.32.1)
  - anchor-spl version matches anchor-lang automatically
- **Learnings for future iterations:**
  - ahash pin was needed for Rust < 1.71 due to MSRV issues (solana-labs/solana#33504)
  - Solana platform-tools v1.51+ uses Rust 1.84, so the pin is obsolete
  - seahorse init.rs reads anchor-lang version from generated Cargo.toml, not hardcoded
  - anchor-lang 0.32.x uses solana modular crates ^2, compatible with solana-sdk 2.2.x
  - anchor-lang 1.0 (RC) uses modular crates ^3+ - future upgrade path when stable
---

## 2026-01-21 - US-002
- Verified mollusk-svm SDK 3.0 compatibility - already done in US-000
- Files changed: none (verification only)
- Verification results:
  - mollusk-svm 0.1.3 with solana-sdk 2.2.1 is correctly configured
  - All 12 Mollusk unit tests pass
  - API uses Account type (compatible with SDK 2.2)
  - cargo check passes (typecheck)
- **Learnings for future iterations:**
  - US-002 was effectively completed as part of US-000 dependency upgrade
  - The Mollusk test harness from US-005 + dependency upgrade from US-000 covers all US-002 criteria
  - Version compatibility chain: mollusk-svm 0.1.3 ↔ solana-sdk 2.2.1 ↔ Anchor 0.32.x
---

## 2026-01-21 - US-003
- Set up LiteSVM integration test harness in tests/integration/
- Files changed: tests/integration/Cargo.toml, lib.rs, helpers.rs, tests.rs, .gitignore
- Changes made:
  - Added litesvm 0.6.1 with solana modular crates 2.2.x
  - Implemented test utilities: create_litesvm, funded_keypair, execute_tx, anchor_instruction
  - Implemented Anchor discriminator helpers (account + instruction)
  - Added slot warping for time-dependent tests
  - 15 integration tests pass
- Skipped anchor-litesvm: v0.2.0 requires anchor-lang 0.31.1, incompatible with our 0.32.1
- **Learnings for future iterations:**
  - litesvm 0.6.x uses solana modular crates (solana-account, solana-pubkey, etc.) not monolithic solana-sdk
  - solana-sha256-hasher crate provides hash() function for discriminators (solana-hash only has Hash type)
  - LiteSVM's TransactionResult is already Result<..., ...>, don't double-wrap in helpers
  - anchor-litesvm version matrix: 0.2.0 ↔ anchor 0.31.1 ↔ litesvm 0.6.1, 0.3.0 ↔ anchor 1.0-rc ↔ litesvm 0.8.x
  - For Anchor 0.32.x compatibility, implement helpers directly without anchor-litesvm
---

## 2026-01-21 - US-004
- Verified all Seahorse examples compile E2E with SDK 3.0
- Files changed: src/bin/cli/init.rs
- Examples tested:
  - calculator.py ✓
  - constants.py ✓
  - event.py ✓
  - fizzbuzz.py ✓
  - hello.py ✓
  - stored_mutables.py ✓
  - pyth.py ✓
- Fix applied: Pin blake3 to =1.8.2 in generated Cargo.toml
  - blake3 v1.8.3+ requires edition 2024 which requires nightly Cargo
  - Solana BPF toolchain uses stable Cargo and doesn't support edition 2024
- **Learnings for future iterations:**
  - E2E testing flow: `seahorse init` → copy .py → `anchor build`
  - Cargo feature `edition2024` requires nightly - pin dependencies that auto-update to versions requiring it
  - The `anchor-spl/idl-build` warning is cosmetic - programs still build correctly
  - Generated code produces "unused macro" warning for `Loaded!` macro - harmless
---

## 2026-01-20 - US-006
- Verified LiteSVM Rust test harness already complete from US-003
- Files changed: none (verification only)
- Verification results:
  - litesvm 0.6.1 dependency in tests/integration/Cargo.toml ✓
  - tests/integration/ directory structure exists ✓
  - Test utilities implemented: program_account, find_pda, anchor_discriminator ✓
  - litesvm_with_program helper exists for loading .so files ✓
  - 15 integration tests pass ✓
  - cargo check passes (typecheck) ✓
- **Learnings for future iterations:**
  - US-006 was effectively completed as part of US-003 (LiteSVM setup)
  - The litesvm_with_program helper loads .so files when available (US-008 will provide them)
  - Test helpers cover: account creation, PDA derivation, discriminator calculation, slot warping
---

## 2026-01-21 - US-007
- Set up TypeScript test harness in tests/ts/
- Files changed: tests/ts/package.json, tsconfig.json, vitest.config.ts, src/helpers.ts, src/helpers.test.ts, .gitignore
- Dependencies installed:
  - litesvm 0.4.0 (npm package uses native bindings to Rust litesvm)
  - @coral-xyz/anchor 0.32.0
  - anchor-litesvm 0.2.1
  - vitest 2.1.x for test runner
- Helpers implemented (matching Rust helpers):
  - liteSvmWithProgram: Load .so file into LiteSVM
  - fundedKeypair, fundedKeypair10Sol: Create funded accounts
  - anchorInstruction: Build instructions with discriminator
  - instructionDiscriminator, accountDiscriminator: Anchor hash calculation
  - signerMeta, writableMeta, readonlyMeta: Account meta helpers
  - executeTx, executeTxMulti: Transaction execution
  - findPda: PDA derivation
  - warpToSlot: Slot warping for time-dependent tests
  - rentExemptLamports: Rent calculation
- 18 TypeScript tests pass
- typecheck passes
- **Learnings for future iterations:**
  - litesvm npm package has `addProgramFromFile(pubkey, path)` for loading .so by path, `addProgram(pubkey, bytes)` for bytes
  - npm anchor-litesvm 0.2.x depends on @coral-xyz/anchor ^0.31.1, but works with 0.32 at runtime
  - vitest globals mode requires `types: ["vitest/globals"]` in tsconfig.json
  - TypeScript test helpers should use `bigint` for lamports to match litesvm JS bindings
  - Run `npm test` from tests/ts/ directory (no root package.json)
---

## 2026-01-21 - US-008
- Created test program compilation pipeline: scripts/build-test-programs.sh
- Files changed: scripts/build-test-programs.sh (new), .gitignore (updated)
- Changes made:
  - Script initializes temporary Anchor project in .build-programs/
  - Compiles Seahorse examples to Rust via seahorse compile
  - Parses multi-file format (dot/mod.rs, dot/program.rs, lib.rs sections)
  - Builds each program to .so via anchor build
  - Auto-detects pyth-sdk-solana dependency from source
  - Outputs artifacts to target/deploy/
  - 6 of 7 programs build: calculator, constants, event, fizzbuzz, hello, pyth
  - stored_mutables fails (pre-existing Seahorse compiler issue with Data type)
- **Learnings for future iterations:**
  - Seahorse compiled output format: `// ===== dot/mod.rs =====`, `// ===== dot/program.rs =====`, `// ===== lib.rs =====`
  - The lib.rs section contains complete seahorse_util module with all required types (Mutable, Empty, SeahorseSigner, etc.)
  - Use awk to split multi-file compiled output into proper directory structure
  - Anchor build requires: Cargo.toml, Anchor.toml, and src/ with lib.rs
  - Pin blake3 to =1.8.2 to avoid edition 2024 requirement
  - pyth-sdk-solana version 0.10.6 compatible with anchor-lang 0.32.x
---

## 2026-01-21 - US-009
- Implemented behavior comparison framework in tests/common/
- Files changed: tests/common/Cargo.toml, lib.rs, compare.rs, tests.rs, .gitignore
- Key components:
  - CompareContext: Manages dual LiteSVM instances for Seahorse/Anchor programs
  - InstructionBuilder: Template-based instruction building for both programs
  - Account state diffing: LamportsDiff, DataDiff, DataSizeDiff, OwnerDiff, ExistsDiff
  - CompareResult with colored report output for divergence diagnosis
  - execute_sequence_and_compare() for multi-instruction testing
- 25 unit tests pass, typecheck passes
- **Learnings for future iterations:**
  - Use standalone functions instead of methods when borrowing mutable SVM instances to avoid borrow checker issues
  - LiteSVM 0.6.x send_transaction returns Result<TransactionMetadata, FailedTransactionMetadata> - both have .logs field
  - Account diffing should compare lamports first (most common failure), then owner, then size, then data bytes
  - Colored crate 2.x provides .red(), .green(), .cyan(), etc. for terminal output
  - Clone LiteSVM instances for "dry run" comparison before committing changes to real SVMs
---

## 2026-01-21 - US-010
- Implemented Counter program with full test suite
- Files changed:
  - examples/counter.py (new Seahorse counter)
  - tests/anchor-reference/counter_anchor/ (Anchor reference program)
  - tests/unit/counter_tests.rs (11 Mollusk unit tests)
  - tests/integration/counter_integration.rs (10 LiteSVM integration tests)
  - tests/common/counter_parity.rs (2 behavior parity tests)
- Counter features:
  - initialize: Creates PDA counter account with authority
  - increment: Increases count by 1 with authorization check
  - decrement: Decreases count by 1 with underflow protection
  - set_value: Sets count to arbitrary value with authorization
- Test coverage:
  - Mollusk: Each instruction in isolation, authorization failures, underflow handling
  - LiteSVM: Full workflows, multi-user scenarios, sequential operations
  - Parity: Seahorse vs Anchor produce identical counter values
- **Learnings for future iterations:**
  - Seahorse-generated code requires rent sysvar for account init, Anchor 0.32+ doesn't
  - Must use `svm.expire_blockhash()` between sequential transactions to avoid "AlreadyProcessed"
  - InstructionBuilder API: `.build(program_id)` takes program ID, `.with_program_id()` is for CPI accounts
  - Mollusk cannot test init instructions that require CPI to system program - use LiteSVM instead
  - For parity tests with different account layouts, use separate instruction builders per program type
---

## 2026-01-21 - US-011
- Implemented Hello World program with full test suite
- Files changed:
  - examples/hello_world.py (new Seahorse hello world)
  - tests/anchor-reference/hello_world_anchor/ (Anchor reference program)
  - tests/integration/hello_world_integration.rs (4 LiteSVM integration tests)
  - tests/common/hello_world_parity.rs (2 behavior parity tests)
- Hello World features:
  - say_hello: Logs "Hello world, from Solana smart contract"
  - Requires a signer account (Anchor 0.32+ requires at least one account)
- Test coverage:
  - LiteSVM: Program execution, multiple calls, different signers, compute units
  - Parity: Seahorse vs Anchor produce identical logs
- **Learnings for future iterations:**
  - Base58 program IDs must decode to exactly 32 bytes - "HeLLo111..." was 31 bytes
  - Use valid base58 IDs like "He11oWor1d111111111111111111111111111111111" (43 chars)
  - Anchor 0.32 programs cannot have empty `#[derive(Accounts)]` structs - need at least one account
  - Module name in `#[program] pub mod X` should match crate name (e.g., hello_world_anchor not hello_world)
  - LiteSVM's TransactionResult.logs field contains program logs for verification
---

## 2026-01-21 - US-012
- Implemented Calculator program tests with full test suite
- Files changed:
  - tests/anchor-reference/calculator_anchor/ (Anchor reference program with matching program ID)
  - tests/unit/calculator_tests.rs (14 Mollusk unit tests)
  - tests/integration/calculator_integration.rs (12 LiteSVM integration tests)
  - tests/common/calculator_parity.rs (3 behavior parity tests)
  - tests/unit/Cargo.toml, tests/integration/Cargo.toml, tests/common/Cargo.toml (added test targets)
- Calculator features:
  - init_calculator: Creates PDA calculator account with owner
  - do_operation: Executes ADD, SUB, MUL, DIV operations using enum
  - reset_calculator: Resets display to 0
- Test coverage:
  - Mollusk: Each operation in isolation, edge cases (multiply by zero, truncation), authorization failures, division by zero
  - LiteSVM: Full workflows, negative values, chained operations, authorization tests
  - Parity: Seahorse vs Anchor produce identical display values, same error behavior for div-by-zero and unauthorized access
- **Learnings for future iterations:**
  - Anchor reference programs must use same declare_id! as Seahorse version for parity tests
  - Operation enum serialization: single byte (0=ADD, 1=SUB, 2=MUL, 3=DIV) followed by i64 operand
  - Calculator account layout: discriminator(8) + owner(32) + display(8) = 48 bytes total
  - Seahorse div-by-zero panics with "attempt to divide by zero", can verify with result.is_err()
  - Mollusk tests work for operations on pre-initialized accounts but not for init (needs system program CPI)
---

## 2026-01-21 - US-013
- Implemented PDA derivation patterns with full test suite
- Files changed:
  - examples/pda_patterns.py (new Seahorse PDA patterns)
  - tests/anchor-reference/pda_patterns_anchor/ (Anchor reference program)
  - tests/integration/pda_patterns_integration.rs (14 LiteSVM integration tests)
  - tests/common/pda_patterns_parity.rs (4 behavior parity tests)
  - tests/compiled-examples/pda_patterns.rs (compiled Seahorse output)
- PDA patterns demonstrated:
  - Single seed: GlobalConfig PDA with ["global_config"]
  - User-derived: UserProfile PDA with ["user_profile", user.key()]
  - Multiple seeds: DataRecord with ["data_record", category, record_id]
  - Multiple seeds: Vault with ["vault", owner.key(), vault_id]
  - Bump storage: All accounts store bump from init
- Test coverage:
  - Integration: Init, update, authorization, multiple accounts, bump persistence
  - Parity: Global config workflow, vault deposits, PDA determinism, auth rejection
- **Learnings for future iterations:**
  - Seahorse stores bump via `config.bump = config.bump()` pattern before init
  - Anchor `#[instruction(arg)]` annotation means arg must be in instruction data even if only used for PDA verification
  - For parity tests with instruction data differences, use ProgramType enum to switch serialization
  - Account sizes may differ between Seahorse and Anchor - verify field offsets carefully
  - String serialization in Borsh: 4-byte length prefix (u32 LE) + UTF-8 bytes
---

## 2026-01-21 - US-014
- Implemented Account initialization patterns with full test suite
- Files changed:
  - examples/init_patterns.py (new Seahorse init patterns)
  - tests/anchor-reference/init_patterns_anchor/ (Anchor reference program)
  - tests/integration/init_patterns_integration.rs (17 LiteSVM integration tests)
  - tests/common/init_patterns_parity.rs (4 behavior parity tests)
  - tests/compiled-examples/init_patterns.rs (compiled Seahorse output)
- Init patterns demonstrated:
  - SimpleData: Primitives (u8, u16, u32, u64, i64) with explicit space calculation
  - StringData: Variable-length strings with padding parameter for dynamic allocation
  - ArrayData: Fixed-size arrays [u64; 4] and [bool; 8]
  - ComplexData: Nested struct (Stats with count, total, average)
  - Space verification tests confirm calculated sizes match allocated
  - Rent exemption verified for all account types
- Account sizes:
  - SimpleData: 64 bytes (disc:8 + owner:32 + u8:1 + u16:2 + u32:4 + u64:8 + i64:8 + bump:1)
  - ArrayData: 81 bytes (disc:8 + owner:32 + [u64;4]:32 + [bool;8]:8 + bump:1)
  - ComplexData: 66 bytes (disc:8 + owner:32 + Stats:24 + bool:1 + bump:1)
  - StringData: variable with padding=200 for name(32) + description(128)
- **Learnings for future iterations:**
  - Seahorse doesn't support init_if_needed - only Empty[T].init() for new accounts
  - Use `padding=N` parameter in init() for variable-length data (strings, lists)
  - Arrays cannot be assigned directly in Seahorse - must initialize element by element
  - Nested struct initialization: `data.stats = Stats(count, u64(0), u64(0))`
  - Seahorse doesn't support docstrings in class definitions - use # comments above class
  - Boolean negation uses `not data.is_active` not `!data.is_active`
  - Integer division uses `//` operator in Python (not `/`)
---


## 2026-01-21 - US-015
- Implemented Token Vault (deposit/withdraw) with full test suite
- Files changed:
  - examples/token_vault.py (new Seahorse token vault)
  - tests/anchor-reference/token_vault_anchor/ (Anchor reference program)
  - tests/integration/token_vault_integration.rs (12 LiteSVM integration tests)
  - tests/compiled-examples/token_vault.rs (compiled Seahorse output)
  - tests/integration/Cargo.toml (added SPL Token dependencies)
  - tests/integration/helpers.rs (added SPL Token helpers)
- Token vault features:
  - initialize_vault: Creates vault state PDA and PDA-owned token account
  - deposit: Transfers tokens from user to vault using CPI to Token Program
  - withdraw: Transfers tokens from vault to owner using PDA signer seeds
  - Authorization: Only vault owner can withdraw
  - Total deposits tracked in vault state account
- SPL Token helpers added:
  - create_mint_account(): Create mint account data for LiteSVM
  - create_token_account(): Create token account data for LiteSVM
  - get_ata(): Get associated token address
  - read_token_balance() / read_mint_supply(): Parse account data
- Test coverage (12 tests):
  - Initialization: Single vault, different users with separate vaults
  - Deposit: Single deposit, multiple deposits, zero amount fails
  - Withdraw: Partial, full, zero fails, insufficient funds fails
  - Authorization: Unauthorized withdrawal fails
  - Full workflow: Deposit → withdraw → deposit → verify balances
- **Learnings for future iterations:**
  - Seahorse TokenAccount.transfer(authority, to, amount, signer) uses signer seeds for PDA-signed transfers
  - Compiled Seahorse code marks mint as `#[account(mut)]` - tests must pass mint as writable_meta
  - LiteSVM requires pre-created token accounts via set_account() with properly packed SPL Token state
  - spl_token::state::Account/Mint use solana_program_pack::COption (not Option) for optional pubkeys
  - Program IDs must be valid Base58 that decodes to 32 bytes - generate with `solana-keygen new`
  - Vault bump stored during init via `vault.bump = vault.bump()` pattern before calling init()
---

## 2026-01-21 - US-016
- Implemented Token Minting program with full test suite
- Files changed:
  - examples/token_mint.py (new Seahorse token minting)
  - tests/anchor-reference/token_mint_anchor/ (Anchor reference program)
  - tests/integration/token_mint_integration.rs (12 LiteSVM integration tests)
  - tests/compiled-examples/token_mint.rs (compiled Seahorse output)
  - tests/integration/Cargo.toml (added token_mint test entry)
- Token minting features:
  - create_mint: Creates MintConfig PDA and Token Mint PDA (mint_config as mint authority)
  - mint_tokens: Mints new tokens using PDA signer (only authority can mint)
  - burn_tokens: Burns tokens from token account (token owner can burn their tokens)
  - MintConfig tracks: mint address, authority, decimals, total_minted, total_burned, bump
- Design notes:
  - MintConfig PDA (not the mint itself) is the mint authority - standard program-controlled pattern
  - Mint PDA derived from seeds=['mint', authority]
  - MintConfig PDA derived from seeds=['mint_config', authority]
  - Mint signer seeds use mint_config bump for CPI authorization
- Test coverage (12 tests):
  - Create mint: Different decimals, different authorities with separate mints
  - Mint tokens: Single mint, multiple mints, different recipients, zero fails, unauthorized fails
  - Burn tokens: Partial burn, zero fails, insufficient funds fails
  - Full workflow: Create → mint → burn → mint → verify totals and supply
- **Learnings for future iterations:**
  - Seahorse marks UncheckedAccount as `#[account(mut)]` by default - tests must pass as writable_meta
  - TokenMint.init(authority=mint_config) sets mint_config PDA as the mint authority
  - TokenMint.mint(authority=mint_config, signer=['mint_config', authority, bump]) uses PDA signer
  - TokenMint.burn(authority=owner, holder=source, amount) burns from holder token account
  - Burn is called on TokenMint not TokenAccount: mint.burn(authority, holder, amount)
  - Total supply tracked via SPL Token Mint state, totals tracked in MintConfig for analytics
---

## 2026-01-21 - US-017
- Enhanced ATA patterns tests with behavior parity tests
- Files changed:
  - tests/integration/ata_patterns_integration.rs (fixed account ordering, added parity tests)
  - tests/compiled-examples/ata_patterns.rs (compiled Seahorse output)
  - prd.json (updated US-017 status to passes: true)
- Changes made:
  - Fixed all instruction account ordering (Seahorse adds rent sysvar, Anchor doesn't)
  - Seahorse marks mint as `#[account(mut)]` - tests must pass as writable_meta
  - Added 5 behavior parity tests comparing Seahorse vs Anchor programs:
    - test_parity_pda_derivation: Verify identical PDAs from same seeds
    - test_parity_register_user_account_data: Compare UserAccount field values
    - test_parity_initialize_treasury_account_data: Compare Treasury field values
    - test_parity_transfer_tokens_balances: Verify identical balance changes
    - test_parity_error_conditions: Both reject zero-amount transfers
- Test coverage (18 tests total):
  - Token account creation: Single user, multiple users
  - Transfer: Success, zero fails
  - Register user: Single, multiple users
  - Initialize treasury: Single admin, multiple admins
  - Airdrop: Success, unregistered user fails, zero fails
  - Full workflow: Treasury → register → airdrop → transfer
  - PDA derivation: Determinism, different seeds
  - Parity: 5 tests comparing Seahorse vs Anchor
- **Learnings for future iterations:**
  - Seahorse account order differs from Anchor: Seahorse adds rent sysvar, places token_program last
  - Seahorse marks ALL accounts as mut (mint, treasury, user_account) even when logically read-only
  - Parity tests need different instruction builders for Seahorse vs Anchor (account order, mutability)
  - Account data layout is identical between Seahorse and Anchor (same discriminator, same field order)
  - Both programs derive PDAs identically - use same seeds, same program ID, get same addresses
---

## 2026-01-21 - US-018
- Implemented Token-2022 extensions example demonstrating transfer fee pattern
- Files changed:
  - examples/token_2022.py (new Seahorse fee config tracker)
  - tests/anchor-reference/token_2022_anchor/ (Anchor Token-2022 reference with transfer fee extension)
  - tests/integration/token_2022_integration.rs (17 LiteSVM integration tests)
  - tests/compiled-examples/token_2022.rs (compiled Seahorse output)
  - tests/integration/Cargo.toml (added token_2022 test entry)
  - prd.json (updated US-018 status to passes: true)
- Key design decision: Seahorse doesn't have native Token-2022 support
  - Seahorse only has TokenMint/TokenAccount for legacy SPL Token program
  - Created FeeConfig PDA to track transfer fee configuration
  - Anchor reference uses actual spl-token-2022 crate with transfer fee extension
- Seahorse token_2022.py features:
  - FeeConfig: Stores mint, authority, decimals, transfer_fee_bps, max_fee, total_fees_collected, bump
  - initialize_fee_config: Creates fee config PDA for externally-created Token-2022 mint
  - update_fee_rate: Updates fee rate and max fee (authorization required)
  - calculate_transfer_fee: Demonstrates fee calculation logic (fee = min(amount * bps / 10000, max_fee))
  - record_fee_collection: Tracks collected fees after withdrawal
  - get_fee_info: Displays current fee configuration
- Anchor reference token_2022_anchor features:
  - Uses spl-token-2022 v6 with anchor-spl token_2022_extensions feature
  - create_transfer_fee_mint: Creates Token-2022 mint with transfer fee extension via manual CPI
  - mint_tokens: Mints using PDA authority via token_interface
  - get_transfer_fee_config: Reads extension data from mint account
  - withdraw_withheld_fees: Withdraws collected fees via transfer_fee_instruction
- Test coverage (17 tests):
  - Initialize fee config: Success, max fee (100%), exceeds max fails
  - Update fee rate: Success, unauthorized fails
  - Calculate fee: Normal calculation, max fee capping
  - Record collection: Single/multiple recordings, unauthorized fails
  - Get fee info: Displays configuration
  - Full workflow: Init → calculate → record → update → verify
- **Learnings for future iterations:**
  - Seahorse doesn't support Token-2022 natively - only legacy SPL Token via TokenMint/TokenAccount
  - To add Token-2022 support to Seahorse would require new builtin types (Token2022Mint, Token2022Account)
  - anchor-spl feature is "token_2022_extensions" not "token-interface"
  - spl-token-2022 v6 API: extension::transfer_fee::instruction for fee-related instructions
  - withdraw_withheld_tokens_from_accounts takes &[&Pubkey] for signers (multisig), use empty for PDA authority
  - Token-2022 mint needs manual space calculation with ExtensionType::try_calculate_account_len
  - Token-2022 mint initialization: 1) create_account 2) initialize_transfer_fee_config 3) initialize_mint
---

## 2026-01-21 - US-019
- Implemented Escrow program for trustless token swaps
- Files changed:
  - examples/escrow.py (new Seahorse escrow)
  - tests/anchor-reference/escrow_anchor/ (Anchor reference program)
  - tests/compiled-examples/escrow.rs (compiled Seahorse output)
- Escrow features:
  - make_escrow: Maker deposits Token A, specifies Token B wanted amount
  - take_escrow: Taker sends Token B to maker, receives Token A from vault
  - cancel_escrow: Maker can recover Token A before escrow is taken
- Design decisions:
  - PDA seeds use escrow_id only (not maker + escrow_id)
  - Escrow PDA is the vault token account authority
  - Vault derived with seeds=['vault', escrow_id]
- **Learnings for future iterations:**
  - Seahorse cannot use UncheckedAccount in PDA signer seeds
  - When taker (non-maker) needs to trigger PDA-signed transfer, seeds must not include maker
  - Use simpler seeds (escrow_id only) instead of complex seeds (maker + escrow_id) for multi-party flows
  - Store bump in escrow account and read via escrow.bump for signer seeds
  - Both init and transfer operations can use escrow PDA as authority via signer=['escrow', escrow_id, bump]
---

## 2026-01-21 - US-020
- Implemented comprehensive tests for escrow program
- Files changed:
  - tests/integration/escrow_integration.rs (new - 11 integration tests)
  - tests/integration/Cargo.toml (added escrow_integration test target)
  - tests/common/escrow_parity.rs (new - 4 behavior tests)
  - tests/common/Cargo.toml (added escrow_parity test target)
  - examples/escrow.py (fixed invalid program ID)
  - tests/compiled-examples/escrow.rs (fixed PDA signer seed bug)
  - tests/anchor-reference/escrow_anchor/Cargo.toml (added init-if-needed feature)
- Bug fixes during implementation:
  - Fixed invalid program ID in escrow.py (43-char base58 decoded to 33 bytes, not 32)
  - Fixed PDA signer seeds bug: escrow.escrow_id -> escrow.borrow().escrow_id in signer seeds
  - Added init-if-needed feature to Anchor reference for init_if_needed macro support
- Test coverage:
  - make_escrow: single, multiple escrows, zero amount errors
  - take_escrow: happy path, wrong maker fails, insufficient funds fails
  - cancel_escrow: happy path, unauthorized fails
  - Full workflows: make→take, make→cancel
  - Seahorse behavior verification (state init, workflow, cancel, deterministic PDA)
- **Learnings for future iterations:**
  - Seahorse compiled output uses Mutable<T> wrapper that requires .borrow() for field access
  - When using fields in signer seeds, always use escrow.borrow().field not escrow.field
  - Anchor reference programs using InterfaceAccount<TokenAccount> and ATA constraints have different account order than Seahorse
  - For parity tests between programs with different interfaces, focus on verifying similar behavior rather than byte-for-byte comparison
  - Anchor escrow uses InterfaceAccount + associated_token::authority while Seahorse uses Box<Account<TokenAccount>> + basic token accounts
---

## 2026-01-21 - US-021
- Implemented Token swap AMM (Automated Market Maker) with constant product formula
- Files changed:
  - examples/token_swap.py (new Seahorse AMM)
  - tests/anchor-reference/token_swap_anchor/ (Anchor reference program)
  - tests/compiled-examples/token_swap.rs (compiled Seahorse output)
- AMM features:
  - Pool account: mint_a, mint_b, lp_mint, pool_token_a, pool_token_b, fee_bps, bump
  - initialize_pool: Creates pool state PDA, LP token mint PDA, pool token accounts PDA
  - add_liquidity: Geometric mean for first deposit (sqrt(a*b) - MIN_LIQUIDITY), proportional for subsequent
  - remove_liquidity: Burn LP tokens, receive proportional share of pool tokens
  - swap: Constant product formula with configurable fee (x * y = k)
  - isqrt: Integer square root using Newton's method
- Key design decisions:
  - Pool PDA seeds: ['pool', mint_a, mint_b]
  - Pool is authority for LP mint and pool token accounts
  - Minimum liquidity (100) locked on first deposit to prevent manipulation
  - Slippage protection via min_lp_tokens and min_amount_out parameters
  - Fee in basis points (e.g., 30 = 0.3%)
- **Learnings for future iterations:**
  - Seahorse constants use `CONSTANT = value` not `CONSTANT: type = value`
  - Program IDs must be valid base58 that decodes to exactly 32 bytes (43-44 chars typically)
  - Use integer math throughout to avoid floating point: (a * b) // c instead of a * b / c
  - For multi-token operations, use u64 arithmetic with u128 intermediate for overflow safety
  - Newton's method for isqrt: x = (x + n/x) / 2 until x stabilizes
  - Pin blake3 to =1.8.2 in Anchor Cargo.toml to avoid edition 2024 requirement
---

## 2026-01-21 - US-022
- Implemented comprehensive tests for token swap AMM program
- Files changed:
  - tests/integration/token_swap_integration.rs (new - 20 integration tests)
  - tests/integration/Cargo.toml (added token_swap_integration test target)
  - tests/common/token_swap_parity.rs (new - 5 parity tests)
  - tests/common/Cargo.toml (added token_swap_parity test target)
- Integration test coverage (20 tests):
  - initialize_pool: basic setup, different fees, invalid fee (>=100%) fails
  - add_liquidity: first deposit (sqrt formula), subsequent (proportional), imbalanced deposit, slippage protection, zero amount fails, deposit too small fails
  - remove_liquidity: partial withdrawal, full withdrawal, slippage protection, zero amount fails
  - swap: A to B, B to A, different fee pools, slippage protection, zero amount fails
  - full_amm_workflow: init → add liquidity → swap → add more → remove liquidity
  - constant_product_invariant: verify k never decreases across multiple swaps
- Parity test coverage (5 tests):
  - pool_initialization: verify pool state, LP mint, pool token accounts
  - first_liquidity_deposit_formula: verify LP = sqrt(a*b) - MINIMUM_LIQUIDITY
  - swap_math_constant_product: verify output matches formula with fees
  - lp_token_accounting: verify LP tokens burned correctly on remove
  - deterministic_pda_derivation: verify same seeds produce same PDAs
- **Learnings for future iterations:**
  - Swap math formula: output = (amount_in * (10000 - fee_bps) * reserve_out) / (reserve_in * 10000 + amount_in * (10000 - fee_bps))
  - For fee_bps = 30 (0.3%), swapping 1000 tokens from 10000:10000 pool gives 906 tokens
  - For fee_bps = 10 (0.1%), same swap gives 908 tokens (lower fee = more output)
  - Always verify k = pool_a * pool_b never decreases (fees increase k)
  - Use Pubkey::new_unique() instead of invalid base58 strings for test pubkeys
---

## 2026-01-21 - US-023
- Implemented token staking program with time-based reward calculation
- Files changed:
  - examples/staking.py (new Seahorse staking program)
  - tests/anchor-reference/staking_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/staking_anchor/Cargo.toml (new)
  - tests/compiled-examples/staking.rs (compiled Seahorse output)
  - tests/integration/staking_integration.rs (new - 7 integration tests)
  - tests/integration/Cargo.toml (added staking_integration test target)
  - tests/common/staking_parity.rs (new - 5 parity tests)
  - tests/common/Cargo.toml (added staking_parity test target)
- Staking features:
  - Pool account: authority, stake_mint, reward_mint, stake_vault, reward_rate, total_staked, last_update_slot, bump
  - UserStake account: owner, pool, staked_amount, pending_rewards, last_stake_slot, bump
  - initialize_pool: Creates pool PDA and stake vault PDA (pool is vault authority)
  - create_user_stake: Creates user-specific stake tracking account
  - stake: Transfers tokens to vault, accumulates pending rewards on existing stake
  - unstake: Returns tokens, mints accumulated rewards
  - claim_rewards: Mints rewards without unstaking (stake preserved)
- Reward formula: rewards = staked_amount * reward_rate * slots_elapsed / REWARD_SCALE
  - REWARD_SCALE = 1,000,000 (for precision without floating point)
  - Example: 1000 staked * 100,000 rate * 100 slots / 1,000,000 = 10,000 rewards
- Key design decisions:
  - Pool PDA seeds: ['pool', stake_mint] (not authority + stake_mint for simpler PDA signing)
  - UserStake PDA seeds: ['user_stake', stake_mint, user] (not pool + user)
  - Pool is reward mint authority (can mint rewards)
  - Clock sysvar passed as parameter for slot-based timing
- Test coverage:
  - Integration: initialize_pool, stake, unstake_with_rewards (time warp), claim_rewards_without_unstaking, error cases
  - Parity: pool initialization, stake/pool totals, reward formula verification, claim preserves stake, deterministic PDAs
- **Learnings for future iterations:**
  - Seahorse cannot use Pool account type as seed in PDA derivation (use stake_mint instead)
  - Seahorse cannot use UncheckedAccount in PDA signer seeds (authority must be stored in account)
  - LiteSVM warp_to_slot() advances time for slot-based reward calculations
  - When Seahorse marks an account as `mut` (mutable), tests must use writable_meta() not readonly_meta()
  - Account data offsets must match exactly: disc(8) + fields in order (verify with generated code)
---

## 2026-01-21 - US-024
- Implemented simplified lending protocol with collateralization
- Files changed:
  - examples/lending.py (new Seahorse lending program)
  - tests/anchor-reference/lending_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/lending_anchor/Cargo.toml (new)
  - tests/compiled-examples/lending.rs (compiled Seahorse output)
  - tests/integration/lending_integration.rs (new - 13 integration tests)
  - tests/integration/Cargo.toml (added lending_integration test target)
  - tests/common/lending_parity.rs (new - 5 parity tests)
  - tests/common/Cargo.toml (added lending_parity test target)
- Lending features:
  - Pool account: authority, collateral_mint, borrow_mint, vaults, interest_rate, total_deposited, total_borrowed, last_update_slot, bump
  - UserPosition account: owner, pool, collateral_deposited, borrowed_amount, last_update_slot, bump
  - initialize_pool: Creates pool PDA with collateral and borrow vaults (pool is vault authority)
  - create_user_position: Creates user-specific position tracking account
  - deposit_collateral: Transfers collateral tokens to vault, updates position
  - borrow: Borrows tokens against collateral (150% collateral ratio enforced), accrues interest
  - repay: Repays borrowed amount with accrued interest
  - withdraw_collateral: Returns collateral (maintains collateral ratio)
- Collateral ratio: 150% (15000 BPS / 10000 = 1.5x)
- Interest formula: accrued = borrowed * interest_rate * slots_elapsed / INTEREST_SCALE
  - INTEREST_SCALE = 1,000,000 (for precision without floating point)
  - Example: 1000 borrowed * 1000 rate * 100 slots / 1,000,000 = 100 interest
- Key design decisions:
  - Pool PDA seeds: ['pool', collateral_mint] (simpler than using both mints)
  - Collateral vault seeds: ['collateral_vault', collateral_mint]
  - Borrow vault seeds: ['borrow_vault', collateral_mint]
  - UserPosition seeds: ['user_position', collateral_mint, user]
  - Pool is authority for both vaults (can transfer via PDA signer)
  - Interest accrued before each operation (borrow, repay, withdraw) to maintain accurate debt
- Test coverage:
  - Integration (13 tests): initialize_pool, deposit_collateral, borrow within/exceeds ratio, repay partial/full, withdraw with/without debt, interest accrual, error cases, full workflow
  - Parity (5 tests): pool initialization, collateral ratio enforcement, interest formula, full workflow, deterministic PDA derivation
- **Learnings for future iterations:**
  - For lending protocols, calculate accrued interest BEFORE checking collateral ratio or processing new borrows
  - Use u128 intermediate calculations in Anchor to prevent overflow on interest computation
  - Collateral ratio check: required_collateral = total_debt * COLLATERAL_RATIO_BPS / BPS_DENOMINATOR
  - Multiple vaults (collateral + borrow) can share same PDA authority (the pool account)
  - Repay can be capped to total debt: `if repay_amount > total_debt: repay_amount = total_debt`
  - For withdrawal checks, verify remaining_collateral >= required_collateral (not just deposited >= amount)
---

## 2026-01-21 - US-025
- Implemented token vesting with cliff and linear release schedule
- Files changed:
  - examples/vesting.py (new Seahorse vesting program)
  - tests/anchor-reference/vesting_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/vesting_anchor/Cargo.toml (new)
  - tests/compiled-examples/vesting.rs (compiled Seahorse output)
  - tests/integration/vesting_integration.rs (new - 14 integration tests)
  - tests/integration/Cargo.toml (added vesting_integration test target)
  - tests/common/vesting_parity.rs (new - 5 parity tests)
  - tests/common/Cargo.toml (added vesting_parity test target)
- Vesting features:
  - VestingAccount: authority, beneficiary, mint, vault, total_amount, released_amount, start_slot, cliff_slot, end_slot, is_cancelled, bump
  - create_vesting: Authority creates vesting schedule with cliff period and linear vesting duration
  - claim_vested: Beneficiary claims vested tokens after cliff (linear release formula)
  - cancel_vesting: Authority cancels vesting, vested goes to beneficiary, unvested returns to authority
- Linear vesting formula: vested = total_amount * (current_slot - cliff_slot) / (end_slot - cliff_slot)
  - Before cliff: vested = 0
  - At/after end: vested = total_amount
  - During vesting: linear interpolation based on slots elapsed since cliff
- Key design decisions:
  - Vesting PDA seeds: ['vesting', beneficiary, mint] (per-user per-token)
  - Vault PDA seeds: ['vesting_vault', beneficiary, mint] with vesting as authority
  - Supports no cliff (cliff_slots = 0) - linear vesting starts immediately
  - Partial claims allowed - beneficiary can claim multiple times during vesting period
  - Cancel distributes fairly: unreleased vested to beneficiary, unvested to authority
- Test coverage:
  - Integration (14 tests): create with/without cliff, zero amount/duration fails, claim before cliff fails, claim at cliff, partial claims accumulate correctly, unauthorized claim fails, cancel before cliff/mid-vesting/unauthorized, already cancelled fails, full workflow, claim from cancelled fails
  - Parity (5 tests): initialization state matches, cliff enforcement identical, linear vesting calculation identical, partial claims work correctly, deterministic PDA derivation
- **Learnings for future iterations:**
  - Use `vault = vault.init(...)` not just `vault.init(...)` - Seahorse init returns the initialized account
  - LiteSVM warp_to_slot() is essential for testing time-based vesting calculations
  - Anchor uses u128 intermediate for vesting math to prevent overflow on large amounts
  - Vesting cancellation should calculate vested amount at current time, not just use released_amount
  - Both Seahorse and Anchor use same linear vesting formula: total * elapsed / period (integer division)
  - Store bump in vesting account for PDA signer seeds in claim and cancel instructions
---

## 2026-01-21 - US-026
- Implemented token-based auction program with time-based state transitions
- Files changed:
  - examples/auction.py (new Seahorse auction program)
  - tests/anchor-reference/auction_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/auction_anchor/Cargo.toml (new)
  - tests/compiled-examples/auction.rs (compiled Seahorse output)
  - tests/integration/auction_integration.rs (new - 16 integration tests)
  - tests/integration/Cargo.toml (added auction_integration test target)
  - tests/common/auction_parity.rs (new - 6 parity tests)
  - tests/common/Cargo.toml (added auction_parity test target)
- Auction features:
  - Auction account: seller, auction_id, bid_mint, starting_price, current_bid, highest_bidder, start_slot, end_slot, is_ended, is_claimed, bump
  - create_auction: Creates auction PDA and bid_escrow vault PDA (auction is vault authority)
  - place_bid: Transfers new bid to escrow, refunds previous highest bidder via PDA-signed transfer
  - end_auction: Marks auction as ended (only callable after end_slot)
  - claim_prize: Winner confirms claim, payment transferred from escrow to seller
- Key design decisions:
  - Token-based auction (not SOL) because Seahorse doesn't support PDA-signed lamport transfers
  - Seahorse only has Signer.transfer_lamports() for native SOL, no PDA-signed SOL transfer
  - TokenAccount.transfer(authority, to, amount, signer) works with PDA signer seeds
  - Auction PDA seeds: ['auction', auction_id]
  - Bid escrow vault seeds: ['bid_escrow', auction_id]
  - highest_bidder uses seller.key() as placeholder when no bids (current_bid=0 indicates no real bids)
  - Seahorse doesn't support Pubkey() constructor - can't create default/empty pubkey
- Test coverage:
  - Integration (16 tests): create_auction (basic, different IDs, zero price fails, zero duration fails), place_bid (first bid, outbid with refund, below minimum fails, auction expired fails, bid on ended fails), end_auction (success after expiry, before expiry fails, already ended fails), claim_prize (success, not winner fails, not ended fails, no bids fails), full workflow
  - Parity (6 tests): state initialization, bid token transfer, outbid refund mechanics, time-based auction end, claim payment transfer, deterministic PDA derivation
- **Learnings for future iterations:**
  - Seahorse doesn't have Pubkey() constructor - use a known pubkey as placeholder with state flag (e.g., current_bid=0)
  - For multi-party flows (auction), use escrow pattern with PDA as vault authority
  - TokenAccount.transfer() with signer seeds enables PDA-signed token transfers
  - Token-based auctions avoid the limitation of no PDA-signed SOL transfers in Seahorse
  - LiteSVM warp_to_slot() essential for testing time-based auction logic
  - Anchor's Clock sysvar must be passed as parameter to Seahorse instructions for slot-based timing
---

## 2026-01-21 - US-027
- Implemented NFT minting configuration tracking program
- Files changed:
  - examples/nft_mint.py (new Seahorse NFT config tracking program)
  - tests/anchor-reference/nft_mint_anchor/src/lib.rs (new Anchor reference with Metaplex CPI)
  - tests/anchor-reference/nft_mint_anchor/Cargo.toml (new)
  - tests/compiled-examples/nft_mint.rs (compiled Seahorse output)
  - tests/integration/nft_mint_integration.rs (new - 13 integration tests)
  - tests/integration/Cargo.toml (added nft_mint_integration test target)
- NFT config features:
  - NftConfig account: authority, nft_id (u64 for PDA derivation), mint, name (max 32 chars), symbol (max 10 chars), uri (max 200 chars), is_minted, bump
  - create_nft_config: Creates config PDA with validated metadata (length checks)
  - update_nft_config: Updates metadata before minting (fails after is_minted=true)
  - mark_as_minted: Records mint address, sets is_minted flag (simulates Metaplex mint)
  - get_nft_info: Logs configuration data for verification
- Key design decisions:
  - Seahorse doesn't have native Metaplex Token Metadata support (no CPI wrappers)
  - Parity approach matches Token-2022 pattern: Seahorse tracks config, Anchor does actual CPI
  - PDA seeds: ['nft_config', nft_id] using u64 (can't use UncheckedAccount in Seahorse seeds)
  - Anchor reference includes both full Metaplex mint_nft (metadata + master edition) and parity-compatible instructions
  - mpl-token-metadata v5.0.0 no longer has no-entrypoint feature - removed from Cargo.toml
- Test coverage:
  - Integration (13 tests): create config (basic, multiple IDs, name/symbol too long errors), update config (basic, unauthorized fails, after minting fails), mark_as_minted (basic, unauthorized fails, twice fails), get_nft_info, full workflow, PDA determinism
- **Learnings for future iterations:**
  - Seahorse print statements that use moved values cause Rust borrow errors in compiled output
  - Move print statements BEFORE assignments when the assigned values are used in the print
  - mpl-token-metadata 5.x no longer has no-entrypoint feature - just use plain dependency
  - For features Seahorse doesn't support natively (Metaplex, Token-2022), use config tracking pattern
  - NftConfig uses nft_id: u64 instead of mint key because Seahorse can't use UncheckedAccount in PDA seeds
---

## 2026-01-21 - US-028
- Implemented NFT collection with verified membership tracking program
- Files changed:
  - examples/nft_collection.py (new Seahorse collection tracking program)
  - tests/anchor-reference/nft_collection_anchor/src/lib.rs (new Anchor reference with Metaplex CPI)
  - tests/anchor-reference/nft_collection_anchor/Cargo.toml (new)
  - tests/integration/nft_collection_integration.rs (new - 17 integration tests)
  - tests/integration/Cargo.toml (added nft_collection_integration test target)
- Collection features:
  - Collection account: authority, collection_id (u64 for PDA), collection_mint, name/symbol/uri, total_nfts, verified_nfts, is_finalized, bump
  - CollectionMember account: collection, nft_mint, is_verified, bump
  - create_collection: Creates collection config PDA with validated metadata
  - update_collection: Updates metadata before finalization
  - finalize_collection: Locks collection metadata (prevents further updates)
  - add_nft_to_collection: Registers NFT as member (increments total_nfts)
  - verify_nft: Marks member as verified (increments verified_nfts)
  - unverify_nft: Removes verification status (decrements verified_nfts)
  - get_collection_info/get_member_info: Read-only logging
- Key design decisions:
  - Like US-027, Seahorse tracks collection state since no native Metaplex support
  - Member PDA seeds: ['member', collection.key(), nft_mint.key()]
  - Anchor reference includes full Metaplex CPI (create_collection_nft, mint_to_collection, verify/unverify_collection_metaplex)
  - UnverifyCollection struct uses `collection` and `collection_master_edition_account` field names (not `collection_metadata` and `collection_master_edition`)
- Test coverage:
  - Integration (17 tests): create collection (basic, different IDs, name/symbol too long), update collection (basic, unauthorized, after finalization), finalize collection, add NFT (basic, multiple), verify NFT (basic, unauthorized, twice fails), unverify NFT (basic, unverified fails), full workflow, PDA determinism
- **Learnings for future iterations:**
  - Base58 program IDs must decode to exactly 32 bytes - lowercase 'l' and digit '0' are invalid
  - anchor_spl::metadata struct field names vary: SetAndVerifyCollection uses collection_metadata, UnverifyCollection uses collection
  - Collection finalization locks metadata but still allows adding/verifying NFTs
  - total_nfts and verified_nfts counters enable on-chain collection statistics
---

## 2026-01-21 - US-029 & US-030
- Implemented NFT staking with time-based reward tokens
- Files changed:
  - examples/nft_staking.py (new Seahorse NFT staking program)
  - tests/anchor-reference/nft_staking_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/nft_staking_anchor/Cargo.toml (new)
  - tests/compiled-examples/nft_staking.rs (compiled Seahorse output)
  - tests/integration/nft_staking_integration.rs (new - 9 integration tests)
  - tests/integration/Cargo.toml (added nft_staking_integration test target)
- NFT staking features:
  - Pool account: authority, reward_mint, reward_rate, total_staked, last_update_slot, bump
  - StakeRecord account: owner, pool, nft_mint, nft_vault, staked_at_slot, last_claim_slot, pending_rewards, is_staked, bump
  - initialize_pool: Creates pool state PDA with configurable reward rate (pool is reward mint authority)
  - stake_nft: Transfers NFT to PDA vault, creates stake record (stake_record is vault authority)
  - unstake_nft: Returns NFT to user, mints accumulated rewards via PDA-signed mint
  - claim_rewards: Mints rewards without unstaking (stake preserved)
- Reward formula: rewards = reward_rate * slots_elapsed / REWARD_SCALE
  - REWARD_SCALE = 1,000,000 (for precision without floating point)
  - Per-NFT rewards (not per-token like fungible staking)
- Key design decisions:
  - Pool PDA seeds: ['pool', reward_mint] (not authority) due to Seahorse limitation
  - Seahorse cannot use UncheckedAccount in PDA signer seeds
  - StakeRecord PDA seeds: ['stake_record', nft_mint] (each NFT has one record)
  - StakeRecord is the NFT vault authority (for transfer back on unstake)
  - Pool is the reward mint authority (for minting rewards)
  - NFT vault seeds: ['nft_vault', nft_mint]
- Test coverage (9 tests):
  - initialize_pool: basic setup
  - stake_nft: happy path, user doesn't own NFT fails
  - unstake_nft_with_rewards: uses warp_to_slot() for time-based reward accumulation
  - unstake_not_staked_fails: validation error
  - claim_rewards_without_unstaking: preserves stake, mints rewards
  - claim_rewards_not_staked_fails: validation error
  - full_nft_staking_workflow: stake → warp → claim → warp → unstake
  - pda_derivation_deterministic: verify seeds produce correct addresses
- **Learnings for future iterations:**
  - NFT has supply=1 - stake/unstake transfers amount=1
  - warp_to_slot() advances time for slot-based reward calculation testing
  - Seahorse PDA signer seeds must use parameter types that can be cast to Seed (TokenMint works, UncheckedAccount doesn't)
  - For multi-party flows where PDA must sign, design seeds using available parameter types
  - StakeRecord PDA as vault authority enables unstake without needing pool authority
  - Combined US-029 and US-030 because tests were implemented alongside the main program
---

## 2026-01-21 - US-031
- Implemented NFT marketplace for listing and purchasing NFTs
- Files changed:
  - examples/nft_marketplace.py (new Seahorse marketplace program)
  - tests/anchor-reference/nft_marketplace_anchor/src/lib.rs (new Anchor reference)
  - tests/anchor-reference/nft_marketplace_anchor/Cargo.toml (new)
  - tests/compiled-examples/nft_marketplace.rs (compiled Seahorse output)
- NFT marketplace features:
  - Listing account: seller, listing_id (u64 for PDA), nft_mint, payment_mint, price, is_active, bump
  - list_nft: Creates listing PDA and NFT escrow vault PDA, transfers NFT from seller to escrow
  - buy_nft: Buyer pays seller via token transfer, NFT transferred from escrow to buyer via PDA-signed transfer
  - delist_nft: Seller cancels listing, NFT returned via PDA-signed transfer
  - update_price: Change listing price while active (seller only)
- Key design decisions:
  - Uses SPL tokens for payment (not SOL) since Seahorse supports PDA-signed token transfers but not PDA-signed lamport transfers (same pattern as auction.py)
  - Listing PDA seeds: ['listing', listing_id]
  - NFT escrow vault seeds: ['nft_escrow', listing_id]
  - Listing PDA is the NFT escrow vault authority
  - PDA seeds use listing_id (u64) instead of nft_mint because UncheckedAccount cannot be used in Seahorse PDA seeds
- **Learnings for future iterations:**
  - TokenAccount.init() must have its return value assigned: `nft_escrow = nft_escrow.init(...)` not just `nft_escrow.init(...)`
  - For marketplace-style flows (list/buy/delist), the pattern matches auction: escrow token account with listing/auction PDA as authority
  - PDA-signed token transfers use `token_account.transfer(authority=listing, ..., signer=['listing', listing_id, bump])`
  - SPL token payment is more versatile than SOL for Seahorse programs due to PDA signer support
---

## 2026-01-21 - US-032
- Implemented comprehensive tests for NFT marketplace program
- Files changed:
  - tests/integration/nft_marketplace_integration.rs (new - 17 integration tests)
  - tests/integration/Cargo.toml (added nft_marketplace_integration test target)
  - tests/common/nft_marketplace_parity.rs (new - 5 behavior parity tests)
  - tests/common/Cargo.toml (added nft_marketplace_parity test target)
  - examples/nft_marketplace.py (fixed invalid program ID)
  - tests/anchor-reference/nft_marketplace_anchor/src/lib.rs (fixed program ID)
  - tests/compiled-examples/nft_marketplace.rs (fixed program ID)
- Test coverage:
  - Integration tests (17 tests):
    - list_nft: basic, different listing IDs, zero price fails, non-owner fails
    - buy_nft: happy path, insufficient payment fails, inactive listing fails
    - delist_nft: happy path, unauthorized fails, inactive listing fails
    - update_price: happy path, unauthorized fails, zero price fails, inactive listing fails
    - full_marketplace_workflow_buy: list → update_price → buy
    - full_marketplace_workflow_delist: list → delist
    - pda_derivation_deterministic: verify seeds produce consistent addresses
  - Parity tests (5 tests):
    - parity_listing_initialization: verify listing state on creation
    - parity_buy_transfers_nft_and_payment: verify buy transfers correctly
    - parity_delist_returns_nft: verify delist returns NFT to seller
    - parity_update_price_changes_listing: verify price update works
    - parity_deterministic_pda_derivation: verify PDA consistency
- Bug fix: Changed program ID from invalid 42-char base58 string to valid 44-char address
  - Original: 'MKT1NftMarketP1ace111111111111111111111111' (42 chars → 33 bytes)
  - Fixed: 'Coa8ZSxePf7ZCsDNHE9SRHyZJRWWnZgmanuKVf4ZBM7Q' (44 chars → 32 bytes)
  - Solana pubkeys must be exactly 32 bytes when decoded from base58
- **Learnings for future iterations:**
  - Base58 program IDs must decode to exactly 32 bytes - padding with '1's doesn't work as expected
  - Test pattern for marketplace: list (escrow NFT) → buy (transfer NFT+payment) or delist (return NFT)
  - create_empty_token_account_for_user helper is useful for setting up buyer's NFT token account
  - svm.expire_blockhash() needed between transactions to avoid duplicate signature errors
  - Full workflow tests verify entire user journey, not just individual operations
---

## 2026-01-21 - US-033
- Implemented simple voting program with proposal and vote mechanics
- Files changed:
  - examples/voting.py (new) - Seahorse voting implementation
  - tests/compiled-examples/voting.rs (new) - Compiled Rust output
  - tests/anchor-reference/voting_anchor/src/lib.rs (new) - Anchor reference
  - tests/anchor-reference/voting_anchor/Cargo.toml (new)
  - tests/integration/voting_integration.rs (new) - 15 integration tests
  - tests/integration/Cargo.toml (modified) - Added voting_integration test
  - tests/common/voting_parity.rs (new) - 5 parity tests
  - tests/common/Cargo.toml (modified) - Added voting_parity test
- **Learnings for future iterations:**
  - Seahorse cannot cast Account types as Seeds directly - use stored IDs (e.g., proposal_id) instead of account.key()
  - For double-vote prevention, init a voter record PDA with seeds including user pubkey - fails if already exists
  - Variables defined in if/elif/else blocks cannot be used outside those blocks in Seahorse - put print/log statements inside each branch
  - String fields need padding parameter in init() for variable-length data allocation
  - PDA seeds: ['voter_record', proposal_id, voter] works; ['voter_record', proposal, voter] fails (cannot cast Account as Seed)
  - Time-based testing uses svm.warp_to_slot(slot) and svm.expire_blockhash()
---

## 2026-01-21 - US-034
- Implemented token-weighted governance program with voting power based on token balance
- Files changed:
  - examples/governance.py (new) - Seahorse governance implementation
  - tests/compiled-examples/governance.rs (new) - Compiled Rust output
  - tests/anchor-reference/governance_anchor/src/lib.rs (new) - Anchor reference
  - tests/anchor-reference/governance_anchor/Cargo.toml (new)
  - tests/integration/governance_integration.rs (new) - 18 integration tests
  - tests/integration/Cargo.toml (modified) - Added governance_integration test
  - tests/common/governance_parity.rs (new) - 5 parity tests
  - tests/common/Cargo.toml (modified) - Added governance_parity test
- Governance features:
  - GovernanceConfig account: authority, governance_mint, proposal_threshold, default_voting_duration, next_proposal_id, bump
  - Proposal account: governance, creator, proposal_id, title, description, yes_votes, no_votes, quorum, start_slot, end_slot, is_finalized, result, bump
  - VoterRecord account: voter, proposal, vote_yes, voting_power, voted_at_slot, bump
  - initialize_governance: Creates governance config PDA with configurable threshold and default duration
  - update_governance_config: Modifies proposal threshold and voting duration
  - create_proposal: Creates proposal PDA (requires token threshold), sets up voting period
  - cast_vote: Records weighted vote (voting power = token balance at time of vote)
  - finalize_proposal: Finalizes proposal after voting ends, checks quorum, determines result
- Key design decisions:
  - Proposal PDA seeds: ['proposal', proposal_id] using u64 (not mint because Seahorse can't use Account types as PDA seeds)
  - VoterRecord PDA seeds: ['voter_record', proposal_id, voter] for double-vote prevention
  - Voting power = token balance at time of voting (snapshot approach)
  - Result codes: 0=pending, 1=passed, 2=rejected, 3=quorum_not_met
  - Quorum checking at finalization time
- Bug fixes during implementation:
  - Fixed account meta mutability: TokenAccount and Config accounts are marked #[account(mut)] in Seahorse, tests must use writable_meta() not readonly_meta()
  - Fixed parity tests to use new svm.set_account() pattern instead of direct function calls
- Test coverage:
  - Integration (18 tests): initialize_governance, create_proposal, cast_vote (weighted yes/no), finalize_proposal (pass/reject/quorum_not_met), double_vote_fails, vote_after_end_fails, vote_zero_balance_fails, full_governance_workflow, PDA determinism
  - Parity (5 tests): governance_initialization, weighted_vote_calculation, quorum_enforcement, full_governance_workflow, deterministic_pda_derivation
- **Learnings for future iterations:**
  - Seahorse-generated code marks TokenAccount as #[account(mut)] - tests must use writable_meta() even for read-only token operations
  - For governance patterns, use proposal_id (u64) in PDA seeds rather than trying to use Account types
  - Voting power is determined at vote time - snapshot of token balance recorded in voter record
  - Quorum is total votes (yes + no), not just yes votes
  - Tie result (yes == no) defaults to rejected (conservative approach)
---

## 2026-01-21 - US-035
- Implemented M-of-N multisig wallet with threshold-based transaction execution
- Files changed:
  - examples/multisig.py (new) - Seahorse multisig implementation
  - tests/compiled-examples/multisig.rs (new) - Compiled Rust output
  - tests/anchor-reference/multisig_anchor/src/lib.rs (new) - Anchor reference
  - tests/anchor-reference/multisig_anchor/Cargo.toml (new)
  - tests/integration/multisig_integration.rs (new) - 15 integration tests
  - tests/integration/Cargo.toml (modified) - Added multisig_integration test
  - tests/common/multisig_parity.rs (new) - 5 parity tests
  - tests/common/Cargo.toml (modified) - Added multisig_parity test
- Multisig features:
  - Multisig account: multisig_id, threshold, owner_count, owner_1..owner_10, next_tx_id, bump
  - Transaction account: multisig, tx_id, proposer, recipient, amount, approval_count, is_executed, bump
  - Approval account: approver, transaction, approved_at_slot, bump
  - create_multisig: Creates multisig wallet PDA with N owners and threshold M
  - propose_transaction: Owner proposes SOL transfer (validates owner, increments tx_id)
  - approve: Owner approves via individual approval record PDAs (double-approval prevention)
  - execute: Executes transaction when threshold is met (transfers SOL from vault)
- Key design decisions:
  - Approval PDAs with seeds ['approval', multisig_id, tx_id, approver] - prevents double-approval via init failure
  - Transaction PDA with seeds ['transaction', multisig_id, tx_id] - uses tx_id counter for uniqueness
  - Multisig PDA with seeds ['multisig', multisig_id] - PDA holds SOL for outgoing transfers
  - Fixed owner storage: 10 pubkey fields (owner_1..owner_10) since Seahorse doesn't support dynamic arrays
  - is_owner() helper function checks all 10 owner slots
  - Supports up to 10 owners, configurable threshold
- Bug fixes during implementation:
  - Fixed invalid base58 program ID - original `Mu1ti5ig1111111111111111111111111111111111` (42 chars) was invalid
  - Generated valid program ID `5h7CmLSs5q2ZfWFns5yUjN6BTYVxVxTNddJ2X1yHnWCd` using solana-keygen
  - Added missing idl-build feature to Cargo.toml for Anchor 0.32.x compatibility
- Test coverage:
  - Integration (15 tests): create_multisig, propose_transaction, approve (single, multiple, double-approval fails), execute (threshold met, threshold not met), full workflow, PDA determinism
  - Parity (5 tests): multisig_initialization, approval_counting, double_approval_prevention, threshold_enforcement, deterministic_pda_derivation
- **Learnings for future iterations:**
  - Base58 program IDs must decode to exactly 32 bytes - use solana-keygen to generate valid addresses
  - Anchor 0.32.x requires `idl-build = ["anchor-lang/idl-build"]` feature in Cargo.toml
  - Double-action prevention via PDA init: attempting to init same PDA twice will fail
  - For M-of-N patterns, use separate Approval PDAs per (tx_id, approver) rather than bitmap tracking
  - SOL transfers from PDA use Multisig.transfer_lamports() in Seahorse (Account type method)
  - Fixed-size owner storage is a Seahorse limitation workaround - no Vec/dynamic arrays in account fields
---


## 2026-01-21 - US-036
- Implemented oracle price feed consumer with Pyth-style patterns
- Files changed:
  - examples/oracle_consumer.py (new) - Seahorse oracle consumer implementation
  - tests/compiled-examples/oracle_consumer.rs (new) - Compiled Rust output
  - tests/anchor-reference/oracle_consumer_anchor/src/lib.rs (new) - Anchor reference with Pyth SDK
  - tests/anchor-reference/oracle_consumer_anchor/Cargo.toml (new)
  - tests/integration/oracle_consumer_integration.rs (new) - 9 integration tests
  - tests/integration/Cargo.toml (modified) - Added oracle_consumer_integration test
  - tests/common/oracle_consumer_parity.rs (new) - 5 parity tests
  - tests/common/Cargo.toml (modified) - Added oracle_consumer_parity test
- Oracle consumer features:
  - PriceConfig account: authority, config_id, feed_name, oracle_address, max_staleness_slots, last_price, last_confidence, last_exponent, last_record_slot, update_count, bump
  - PriceThreshold account: config, threshold_id, trigger_above, target_price_scaled, is_triggered, triggered_at_slot, bump
  - create_price_config: Creates price config PDA with oracle address and staleness settings
  - update_price_config: Updates oracle address and staleness settings (authority only)
  - record_price: Records price from oracle with staleness validation
  - create_price_threshold: Creates threshold monitoring for price above/below target
  - check_threshold: Checks if price crossed threshold and marks triggered
  - reset_threshold: Resets triggered threshold (authority only)
  - get_price_info: Displays price info and staleness status
- Key design decisions:
  - Mocked oracle data via instruction parameters (Seahorse can't deserialize arbitrary account layouts)
  - Staleness validation using slot numbers (current_slot - oracle_publish_slot <= max_staleness_slots)
  - Anchor reference includes real Pyth SDK integration (pyth_sdk_solana) as comparison
  - Price threshold uses raw price comparison (trigger_above: price >= target, trigger_below: price <= target)
  - Threshold can only trigger once until reset
- Bug fix during implementation:
  - create_price_threshold needed config_id as explicit instruction parameter for PDA seeds
  - Seahorse compiler generates Anchor constraints that reference instruction parameters, not account fields
- Test coverage:
  - Integration (9 tests): create_price_config, feed_name_too_long, update_price_config, record_price, record_price_stale_fails, create_price_threshold, check_threshold_trigger_above, reset_threshold, full_oracle_consumer_workflow
  - Parity (5 tests): price_config_initialization, staleness_validation, update_counting, threshold_triggering, deterministic_pda_derivation
- **Learnings for future iterations:**
  - Seahorse PDA seeds in `init()` must use instruction parameters, not account field values
  - For oracle patterns, mock data via parameters is necessary since Seahorse can't deserialize external account formats
  - Pyth SDK integration (pyth_sdk_solana) works well in Anchor reference for comparison
  - Staleness checks should use saturating_sub to handle edge cases
  - Threshold triggering is idempotent (already triggered returns early)
---
